import { SpeakableDirective } from './app-speakable';

describe('AppSpeakable', () => {
  it('should create an instance', () => {
    const directive = new SpeakableDirective();
    expect(directive).toBeTruthy();
  });
});
