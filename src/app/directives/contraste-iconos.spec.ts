import { UiStateService } from './../services/ui-state.service';

describe('ContrasteIconos', () => {
  it('should create an instance', () => {
    const directive = new UiStateService();
    expect(directive).toBeTruthy();
  });
});
